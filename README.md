# Novel Site Prototype

This is a minimal prototype of a web novel platform built with Next.js 14 and Tailwind CSS.

## Development

With Node.js installed, run:

```bash
npm install # install dependencies (internet required)
npm run dev  # start development server
```

The site provides a simple book list and detail pages using sample data stored in `src/data.ts`.
